<UserControl x:Class="MonitorSystem.ItMonitor.PropertyView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:sdk="http://schemas.microsoft.com/winfx/2006/xaml/presentation/sdk"
    mc:Ignorable="d"
    d:DesignHeight="300" d:DesignWidth="400">
    
    <Grid x:Name="LayoutRoot" Background="White">
        <sdk:DataGrid AutoGenerateColumns="False"  Grid.Row="2" Grid.Column="1" 
                      ItemsSource="{Binding  ListChanns}" Name="gvPropertys">
            <sdk:DataGrid.Columns>
                <sdk:DataGridTextColumn Header=" 通道名称"   MinWidth="150"  MaxWidth="150"  Binding="{Binding ChannelName}" />
                <sdk:DataGridTextColumn Header=" 值"   MinWidth="120"  MaxWidth="120"   Binding="{Binding CurrentValue}" />
            </sdk:DataGrid.Columns>
        </sdk:DataGrid>
    </Grid>
</UserControl>
